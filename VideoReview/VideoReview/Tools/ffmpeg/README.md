Put portable FFmpeg binaries here.

Expected Windows layout:

```text
Tools/ffmpeg/bin/ffmpeg.exe
Tools/ffmpeg/bin/ffprobe.exe
```

Files under `Tools/ffmpeg` are copied to the app output folder during build.
